function alpha = Alpha(mx,my,C,h) 
    delX = h;
    delY = h;
    alphaInit = 0;  %Have to figure out a good way to guess alpha
    
    %Determine f2x for area calc
    if (alphaInit-mx*delX) > 0
        f2x = (alphaInit-mx*delX)^2;
    else
        f2x = 0;
    end
    
    %Determine f2y for area calc
    if (alphaInit-my*delY) > 0
        f2y = (alphaInit-mx*delY)^2;
    else
        f2y = 0;
    end
    
    %Find Area of ABFGD
    %Tryggvason Eq 5.22
    A = (1/(2*mx*my))*((alpha^2)-f2x-f2y);
    
    %Calculate alpha using root finding
    g = @(a) A-delX*delY*C;
    alpha = fzero(g,0.5);
end
